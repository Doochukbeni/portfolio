import HomeProject from "@/components/(projects)/HomeProject";

const page = () => {
  return (
    <div className="min-h-full h-screen">
      <HomeProject />
    </div>
  );
};

export default page;
